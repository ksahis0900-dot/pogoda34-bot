# 🌤 ПОГОДА 34 — Telegram Bot

Бот погоды для Волгоградской области с функцией "Как одеть ребенка", рассылкой прогноза и фото городов.
Работает на **Python 3.11 + aiogram 3.x**.

## 🚀 Особенности
- Текущая погода и прогноз на 5 дней (OpenWeather).
- Умные советы по одежде для детей.
- Красивые фото городов (JPG файлы).
- Подписка на утреннюю (7:00) и вечернюю (18:00) рассылку.
- База данных подписчиков (SQLite).
- Защита от ошибок (бот не падает, если фото не найдено).

## � Установка и запуск

1. **Клонировать репозиторий:**
   ```bash
   git clone https://github.com/ksahis0900-dot/pogoda34-bot.git
   cd pogodada34-bot
   ```

2. **Создать окружение и установить зависимости:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Mac/Linux
   venv\Scripts\activate     # Windows
   pip install -r requirements.txt
   ```

3. **Настроить `.env`:**
   Создайте файл `.env` на основе `.env.example`:
   ```bash
   BOT_TOKEN=ВАШ_ТОКЕН_ТЕЛЕГРАМ
   OPENWEATHER_API_KEY=ВАШ_КЛЮЧ_OPENWEATHER
   admin_id=ВАШ_ID
   ```

4. **Запустить бота локально:**
   ```bash
   python bot.py
   ```

## � Как добавить/изменить фото городов

**ВАЖНО:** Бот работает на Linux (Render), поэтому имена файлов должны быть **строго на латинице** (английскими буквами) и без пробелов!

1. **Подготовьте фото:**
   - Формат: **JPG** (лучше всего, маленький вес).
   - Размер: Сжимайте до **200-300 КБ** (иначе бот будет долго грузить их в Телеграм).
   - Имя файла: `volgograd.jpg`, `kamyshin.jpg` (не `Волгоград.jpg`!).

2. **Добавьте файл в папку `images/`**.

3. **Пропишите путь в `bot.py`:**
   В поиске найдите словарь `CITY_PHOTOS` и добавьте путь:
   ```python
   "lat=48.708&lon=44.513": ["images/volgograd.jpg"], 
   ```

4. **Загрузите на сервер:**
   ```bash
   git add images bot.py
   git commit -m "Update photo for Volgograd"
   git push
   ```

## � Как добавить новый город

1. Узнайте координаты города (широта и долгота).
2. В файле `bot.py`:
   - Добавьте запись в словарь `CITIES` (название, эмодзи).
   - Добавьте запись в `CITY_PHOTOS` (путь к картинке).
3. Перезапустите бота.

## ☁️ Деплой на Render.com

Бот настроен для работы на Render (Free Tier).
- **Environment:** Python 3.
- **Build Command:** `pip install -r requirements.txt `
- **Start Command:** `python bot.py`
- Переменные окружения (`env vars`) добавляются в настройках сервиса Render.
