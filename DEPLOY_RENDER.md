# 🚀 Деплой бота на Render.com

## Почему Render?
- ✅ **Бесплатный тариф** с 750 часами в месяц
- ✅ **Работает 24/7** без необходимости держать компьютер включенным
- ✅ **Без VPN** - бот доступен из любой точки мира
- ✅ **Автоматический перезапуск** при сбоях
- ✅ **Webhook режим** - бот мгновенно отвечает на сообщения

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

1. **Убедитесь, что у вас есть Git репозиторий:**
   ```bash
   git status
   ```

2. **Если репозитория нет, создайте его:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Создайте новый репозиторий (например, `pogoda34-bot`)
   - НЕ добавляйте README, .gitignore или лицензию

4. **Подключите локальный репозиторий к GitHub:**
   ```bash
   git remote add origin https://github.com/ваш-username/pogoda34-bot.git
   git branch -M main
   git push -u origin main
   ```

### Шаг 2: Регистрация на Render

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через GitHub (это упростит подключение репозитория)

### Шаг 3: Создание Web Service

1. В панели Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `pogoda34-bot`
3. Заполните настройки:

   **Основные настройки:**
   - **Name:** `pogoda34-bot` (или любое другое имя)
   - **Region:** Frankfurt (EU Central) - ближе к России
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`

   **План:**
   - Выберите **"Free"** (бесплатный)

### Шаг 4: Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

1. **BOT_TOKEN**
   - Value: `8527373588:AAGcjWQtX7VfMvPe4p3bBDJ-0-DUpasy-m8`

2. **OPENWEATHER_API_KEY**
   - Value: `bafd7faf0a523d40f16892a82b062065`

3. **RENDER_URL**
   - Value: `https://pogoda34-bot.onrender.com` (замените на ваш URL)
   - ⚠️ **ВАЖНО:** Сначала создайте сервис, скопируйте URL (он будет вида `https://ваше-имя.onrender.com`), затем добавьте эту переменную

### Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Render автоматически:
   - Склонирует ваш репозиторий
   - Установит зависимости из `requirements.txt`
   - Запустит бота командой `python bot.py`

3. **Дождитесь завершения деплоя** (обычно 2-3 минуты)
4. Статус должен стать **"Live"** (зеленый)

### Шаг 6: Получение URL и обновление переменной

1. После успешного деплоя скопируйте URL вашего сервиса (например, `https://pogoda34-bot.onrender.com`)
2. Перейдите в **Environment** → **Environment Variables**
3. Добавьте или обновите переменную **RENDER_URL** с вашим URL
4. Сохраните изменения - сервис автоматически перезапустится

### Шаг 7: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Бот должен ответить с меню выбора городов

## 🔍 Проверка логов

Если бот не работает:

1. В панели Render перейдите на вкладку **"Logs"**
2. Найдите строку: `Setting webhook: https://...`
3. Проверьте наличие ошибок

## ⚠️ Важные моменты

### Засыпание бесплатного сервиса
- Бесплатный план Render засыпает после **15 минут неактивности**
- При получении webhook от Telegram сервис **автоматически просыпается**
- Первый ответ может занять **30-60 секунд** после пробуждения
- Последующие ответы будут мгновенными

### Автоматическое обновление
При каждом `git push` в ваш репозиторий Render автоматически:
1. Обнаружит изменения
2. Пересоберет приложение
3. Перезапустит бота

## 🔄 Обновление бота

Когда вы вносите изменения в код:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически задеплоит новую версию!

## 🆘 Решение проблем

### Бот не отвечает на /start
1. Проверьте логи в Render
2. Убедитесь, что webhook установлен правильно
3. Проверьте, что RENDER_URL совпадает с реальным URL сервиса

### Ошибка "Webhook not set"
1. Перезапустите сервис в Render (Manual Deploy → Clear build cache & deploy)
2. Проверьте переменную RENDER_URL

### Бот долго отвечает
- Это нормально для бесплатного плана после пробуждения
- Первый ответ: 30-60 секунд
- Последующие ответы: мгновенно

## 📊 Мониторинг

В панели Render вы можете отслеживать:
- **Metrics:** использование CPU, памяти, трафика
- **Logs:** все логи бота в реальном времени
- **Events:** история деплоев и перезапусков

## 🎉 Готово!

Теперь ваш бот работает 24/7 без необходимости:
- ❌ Держать компьютер включенным
- ❌ Использовать VPN
- ❌ Беспокоиться о перезапусках

Бот автоматически:
- ✅ Отвечает на сообщения пользователей
- ✅ Отправляет рассылки в 07:00 и 18:00 МСК
- ✅ Перезапускается при сбоях
- ✅ Обновляется при изменении кода
