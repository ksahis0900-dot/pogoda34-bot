name: Keep Alive

on:
  schedule:
    - cron: '*/8 * * * *'  # Run every 8 minutes
  workflow_dispatch:      # Allow manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run: |
          curl -I https://pogoda34-bot.onrender.com/
          echo "Pinged pogoda34-bot.onrender.com"
